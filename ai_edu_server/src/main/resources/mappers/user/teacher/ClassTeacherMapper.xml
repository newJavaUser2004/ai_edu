<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.coder_mart_server.work_publish_modules.teachers.mappers.ClassTeacherMapper">

    <select id="permission" resultType="java.lang.Boolean">
        SELECT EXISTS(
                       SELECT 1
                       FROM t_class
                       WHERE class_id = #{classId}
                         AND user_id  = #{userId}
                         AND deleted  = 0
                       LIMIT 1
                   )
    </select>
</mapper>